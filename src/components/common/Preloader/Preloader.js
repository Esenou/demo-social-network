import React from 'react';
import preloader from '../../../asserts/images/preloader.gif';

let Preloader = (props) => {

    return <div >
        <img src={preloader} />
    </div>
}
export default Preloader;
